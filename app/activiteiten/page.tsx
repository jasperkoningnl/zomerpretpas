import { ActivityCard } from "@/components/ActivityCard";
import { FilterForm } from "@/components/FilterForm";
import { activiteiten, filterActiviteiten, momentenVanActiviteit } from "@/lib/activiteiten";
export default async function ActiviteitenPage({ searchParams }: { searchParams: Promise<Record<string, string>> }) { const sp = await searchParams; const gefilterd = filterActiviteiten(activiteiten, { wijken: sp.wijk ? [sp.wijk] : undefined, soorten: sp.soort ? [sp.soort] : undefined, leeftijd: sp.leeftijd ? Number(sp.leeftijd) : undefined, toegankelijkheden: sp.toegankelijkheid ? [sp.toegankelijkheid] : undefined, gratis: sp.gratis === "1", datum: sp.datum || undefined }); const groups = new Map<string, typeof gefilterd>(); for (const a of gefilterd) { const key = `${a.titel}|${a.locatie ?? ""}`; groups.set(key, [...(groups.get(key) ?? []), a]); } return <><h2>Alle activiteiten</h2><FilterForm /><p>{groups.size} resultaten uit {gefilterd.length} activiteitregels.</p><div className="grid">{[...groups.values()].map((groep) => <ActivityCard key={groep[0].id} activiteit={groep[0]} count={groep.reduce((n, a) => n + Math.max(1, momentenVanActiviteit(a).length), 0)} />)}</div></>; }
