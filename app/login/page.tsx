"use client";
import { createClient, supabaseConfigured } from "@/lib/supabase";
export default function Login() { async function google() { await createClient().auth.signInWithOAuth({ provider: "google", options: { redirectTo: `${location.origin}/auth/callback` } }); } async function magic(formData: FormData) { const email = String(formData.get("email")); await createClient().auth.signInWithOtp({ email, options: { emailRedirectTo: `${location.origin}/auth/callback` } }); alert("Controleer je e-mail voor de loginlink."); } return <section className="card"><h2>Inloggen</h2>{!supabaseConfigured() ? <p>Supabase is nog niet ingesteld. Voeg NEXT_PUBLIC_SUPABASE_URL en NEXT_PUBLIC_SUPABASE_ANON_KEY toe in Vercel.</p> : null}<button className="button" onClick={google}>Login met Google</button><form action={magic}><p><input name="email" type="email" placeholder="E-mailadres" required /></p><button className="button">Stuur magic link</button></form></section>; }
